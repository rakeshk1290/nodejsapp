const USER_EXISTS_ALREADY = 'User already exists'
const USER_DOES_NOT_EXIST = 'User doesn\'t exists'
const WRONG_PASSWORD = 'Wrong password'
const INVALID_TOKEN = 'Invalid token'
module.exports = {
  USER_DOES_NOT_EXIST,
  USER_EXISTS_ALREADY,
  WRONG_PASSWORD,
  INVALID_TOKEN
}
